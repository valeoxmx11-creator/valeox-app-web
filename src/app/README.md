# App Layer (Next.js)

- `(public)`: public website routes.
- `(auth)`: auth UX and flows.
- `(admin)`: protected admin pages.
- `api/`: BFF route handlers.

Keep route files focused on request/response and rendering. Delegate business behavior to domains/modules.
