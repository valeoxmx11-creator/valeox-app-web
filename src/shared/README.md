# Shared Layer

Reusable primitives with strict boundaries:
- `config/`: env and runtime config
- `components/`: design-system-level UI primitives
- `utils/`: pure utility helpers
- `types/`: cross-cutting type aliases

No business-specific logic belongs in `shared`.
