# Core runtime
NODE_ENV=development
NEXT_PUBLIC_APP_URL=http://localhost:3000

# Database (PostgreSQL)
DATABASE_URI=postgresql://postgres:postgres@localhost:5432/valeox

# Payload CMS
PAYLOAD_SECRET=replace_with_minimum_32_characters_secret
